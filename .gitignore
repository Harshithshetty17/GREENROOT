# Byte-compiled artefacts
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Runtime audit ledger (regenerated by the application on first launch)
crop_recommendations.db
crop_recommendations.db-wal
crop_recommendations.db-shm

# Derived XAI surrogate cache (refitted automatically in ~3 s if absent).
# The three protected artefacts in models/ are tracked; this one is not.
models/xai_surrogate.pkl

# Credentials. Nothing here uses these yet, but the moment accounts land,
# an API key or a password hash committed by accident cannot be un-published
# from a public repository. Ignored in advance, deliberately.
.streamlit/secrets.toml
.env
*.pem
*.key

# Tooling caches
.pytest_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
